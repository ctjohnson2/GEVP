this c++ code is still under development, but to compile one needs itpp libraries and gsl


python script fits principle correlators just fine but is slow
MATLAB script fits and plots single principle correlators, much faster
C++ fits multiple simultaneously in openmp (this is the version called by python GEVP.py but one can change that) 
